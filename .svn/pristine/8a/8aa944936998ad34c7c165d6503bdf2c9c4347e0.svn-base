module.exports=Object.freeze({
    FIND_LOGIN_USER:'SELECT COUNT(*) as isAvail FROM MA_USER WHERE USERNAME=? AND PASSWORD=?',
    FIND_VESSEL_DETAILS:'SELECT * FROM MA_VESSEL_INFO AS VESSEL_INFO WHERE COMPANY_ID=?',
    FIND_AUDIT_CATEGORY:'SELECT * FROM MA_AUDIT_CATEGORY',
    FIND_AUDIT_CODE:'SELECT * FROM MA_AUDIT_CODE',
    FIND_AUDIT_DEFICIENCY:'SELECT * FROM MA_AUDIT_DEFICIENCY',
    FIND_AUDI_CODE_REF:'SELECT * FROM MA_AUDI_CODE_REF',
    FIND_AUDIT_INFO:'SELECT * FROM MA_AUDIT_INFO',
    FIND_AUDIT_INTERVAL:'SELECT * FROM MA_AUDIT_INTERVAL',
    FIND_AUDIT_TYPE:'SELECT * FROM MA_AUDIT_TYPE',
    FIND_COMPANY:'SELECT * FROM MA_COMPANY',
    FIND_DEFICIENCY_CAUSE:'SELECT * FROM MA_DEFICIENCY_CAUSE',
    SAVE_DEFICIENCY:'INSERT INTO AUDIT_DEFICIENCY_REF (TXN_SEQ_NO,DEFICIENCY_ID,REMARK,ACCEPT_CAP_BY_AUDITOR,DETAILS_OF_DEFICIENCY,TARGET_DATE,CORRECTIVE_ACTION_PLAN,COMMENTS_BY_AUDITOR,SHIP_REVISED_CAP,ACCEPT_REVISED_CAP_BY_AUDITOR,CLOSED_DATE,PREVENTIVE_ACTION,ERROR_CHECK,UPDATED_PLACE,USER_NAME,DATE_INS) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)',
    FIND_DEF:'SELECT * FROM MA_AUDIT_INFO INFO,MA_AUDIT_DEFICIENCY MA_DEF,AUDIT_DEFICIENCY_REF DEF_REF WHERE DEF_REF.DEFICIENCY_ID = MA_DEF.DEFICIENCY_ID AND MA_DEF.AUDIT_TRACKING_NUMBER = INFO.AUDIT_TRACKING_NUMBER AND INFO.AUDIT_CATEGORY = ? AND INFO.AUDIT_TYPE = ? AND INFO.AUDIT_INTERVAL = ?'

})